{
  "name": "fielog",
  "version": "0.1.0",
  "description": "Fieldlog — write anywhere, settle later. Offline-first append-log + SQLite read-model + sync.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "build": "tsc -p tsconfig.json"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.4.1",
    "bun-types": "^1.4.1",
    "typescript": "^7.0.2"
  }
}
